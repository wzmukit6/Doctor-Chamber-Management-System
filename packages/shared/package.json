{
  "name": "@chamber/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared permission model, validation schemas, error codes and API types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
